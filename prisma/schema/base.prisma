// Base Prisma configuration & shared enums

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

enum Role {
  BUYER
  SELLER
  SUPER_ADMIN
}

enum Incoterm {
  FOB
  CIF
  EXW
  DDP
  DAP
  CFR
  FCA
}
