// ============================================================================
// 1. ENUMS (STRICT DATA INTEGRITY FROM database.sql)
// ============================================================================

enum UserRole {
  BUYER
  SELLER
  SUPER_ADMIN

  @@map("user_role")
}

enum UserStatus {
  ACTIVE
  SUSPENDED
  PENDING_VERIFICATION

  @@map("user_status")
}

enum RfqPriorityLevel {
  NORMAL
  HIGH
  EXCLUSIVE

  @@map("rfq_priority_level")
}

enum AnalyticsTier {
  NONE
  BASIC
  FULL

  @@map("analytics_tier")
}

enum SubscriptionStatus {
  ACTIVE
  CANCELLED
  PAST_DUE
  TRIALING

  @@map("subscription_status")
}

enum ProductStatus {
  DRAFT
  PUBLISHED
  OUT_OF_STOCK
  ARCHIVED

  @@map("product_status")
}

enum ShippingMethodType {
  STANDARD
  EXPRESS
  FREIGHT

  @@map("shipping_method_type")
}

enum PaymentStatusType {
  PENDING
  PAID
  FAILED
  REFUNDED
  PARTIALLY_REFUNDED

  @@map("payment_status_type")
}

enum FulfillmentStatusType {
  PROCESSING
  CONFIRMED
  SHIPPED
  DELIVERED
  CANCELLED
  DISPUTED

  @@map("fulfillment_status_type")
}

enum RfqStatusType {
  OPEN
  CLOSED
  AWARDED
  EXPIRED

  @@map("rfq_status_type")
}

enum QuoteStatusType {
  PENDING
  ACCEPTED
  REJECTED
  WITHDRAWN

  @@map("quote_status_type")
}

enum PayoutStatusType {
  PENDING
  APPROVED
  PROCESSED
  REJECTED

  @@map("payout_status_type")
}

enum LedgerTransactionType {
  SUBSCRIPTION_FEE
  MARKETPLACE_COMMISSION
  SELLER_PAYOUT
  REFUND_DEDUCTION

  @@map("ledger_transaction_type")
}
